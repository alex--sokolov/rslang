import { updateUserWord } from '../../components/api/api';

export function lestenCheckboxes() {
  const hardCheckboxElement = document.getElementById('hard-word-checkbox') as HTMLInputElement;
  const learnedCheckboxElement = document.getElementById('learned-word-checkbox') as HTMLInputElement;

  // difficultCheckboxElement.addEventListener('change', )
}
